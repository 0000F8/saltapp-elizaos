node_modules/
dist/
# Not committed: it currently resolves salt-agent-sdk via a local sibling-
# checkout path (../salt-agent-sdk isn't published at 0.7.1 yet -- see
# HANDOFF.md), which isn't reproducible for anyone who clones only this
# repo. Regenerate locally; commit a real lockfile once salt-agent-sdk
# 0.7.1+ is on npm and `npm install` resolves it from the registry.
package-lock.json
*.log
.env
.env.*
!.env.example
coverage/
.DS_Store
# Default SALT_STATE_DIR (socket-mode poll cursor/dedupe persistence, see
# config.ts) -- runtime state, never source. A test that doesn't inject
# MemoryCursorStore()/MemoryDedupeStore() into SaltServiceDeps would
# otherwise write real files here.
/data/
